# 发布到 GitHub 指引（小白版）

## 第 1 步：注册 GitHub 账号（5 分钟）

1. 打开 https://github.com （网络需能访问 GitHub，国内可能需要梯子）
2. 点右上角 Sign up，填邮箱、密码、用户名（username），完成人机验证
3. 到邮箱点确认链接，注册完成

## 第 2 步：创建仓库（2 分钟）

1. 登录后点右上角 "+" 号 → "New repository"
2. Repository name 填：kronos-trading-app
3. 可见性选 Public（你已选择公开）
4. **不要**勾选 "Add a README file"（我们本地已有）
5. 点 Create repository

创建后页面会显示一个地址，形如：
   https://github.com/你的用户名/kronos-trading-app.git

## 第 3 步：把地址发给我

把上面的 git 地址复制发给我（例如 https://github.com/xxx/kronos-trading-app.git），
我会自动把本地代码推上去。

## （可选）生成 Token 让工具全程代办

如果你希望我直接用你的账号创建仓库并推送，需要生成 Personal Access Token：
1. GitHub → 右上角头像 → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. 勾选 repo（仓库全部权限），有效期选 90 天
4. 生成后复制 ghp_ 开头的 token 发给我
   （注意：token 只在生成时显示一次，请保密，用完可删）

## 之后想更新代码

仓库建好后，以后每次改完代码跟我说"推一下"，我会自动 commit + push。